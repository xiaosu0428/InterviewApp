<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.xiaosu.interviewapp">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activity.question.practice.QuestionPracticeActivity"
            android:launchMode="singleTask"/>
        <activity android:name=".ui.activity.question.myquestion.MqItemActivity" />
        <activity
            android:name=".ui.activity.question.createmq.MajorChooseActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".ui.activity.category.science.CategoryXMajorActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".ui.activity.question.createmq.CreateMqActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".ui.activity.question.myquestion.MyQuestionActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".ui.activity.main.IndexActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".ui.activity.login.RegisterActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".ui.activity.login.LoginActivity"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>